{
  "description": "A status harvest job definition (XXX only validates type)",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "enum": ["status"],
      "description": "Indicates bulk XML job."
    }
  },
  "required": ["type"],
  "additionalProperties": true
}
