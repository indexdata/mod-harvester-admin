{
  "description": "Harvest job log statements as JSON.",
  "type": "object",
  "properties": {
    "logLines": {
      "description": "Collection of Log4J log statements.",
      "type": "array",
      "items": {
        "type": "object",
        "$ref": "logLine.json"
      }
    }
  }
}