{
  "description": "instructions to start a harvest job immediately",
  "type": "object",
  "properties": {
    "harvestableId": {
      "type": "integer",
      "description": "unique identifier of the harvest configuration to start a job with"
    },
    "force": {
      "type": "boolean",
      "description": "if true, start the job even if an already running job is thereby aborted"
    }
  },
  "additionalProperties": false
}