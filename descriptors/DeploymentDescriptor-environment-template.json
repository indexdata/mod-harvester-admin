{
  "srvcId" : "${artifactId}-${version}",
  "descriptor" : {
    "exec" : "java -jar ../mod-harvester-admin/target/mod-harvester-admin-fat.jar -Dhttp.port=%p",
    "env": [
      { "name" : "harvester.host", "value" : "localhost" },
      { "name" : "harvester.port", "value" : "8080" }
    ]
  }
}
