{
  "srvcId": "${artifactId}-${version}",
  "nodeId": "localhost",
  "descriptor": {
    "exec": "java -Dport=%p -DlogLevel=INFO -jar ../mod-harvester-admin/target/mod-harvester-admin-fat.jar embed_postgres=true"
  }
}
